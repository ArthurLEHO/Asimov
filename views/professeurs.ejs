<%- include('header') %>
    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>ASIMOV</title>
    </head>

    <body>
        <form method="get" action="">
            <div>
                <tb id="bdd" class="tb">
                    <h5>
                    <% dataTotale.forEach((data, index)=>{%> <!--Utilisation du framework Express.JS synchro à la bdd -->
                        <tr id=<%=data.ps_id%>>
                            <%= "Nom:" + data.ps_nom + " |"%>
                            <%= "Prénom:" + data.ps_prenom + " |"%>
                            <%= "Matière:" + data.mt_nom + " |"%>
                        </tr><p></p>
                    <%})%>
                    </h5>
                </tb>
            </div>
        </form>
    </body>

    </html>
    <%- include('footer') %>